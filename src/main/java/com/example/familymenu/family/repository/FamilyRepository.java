package com.example.familymenu.family.repository;
import com.example.familymenu.family.domain.*; import java.util.*;
public interface FamilyRepository { Family createForUser(String name, Long userId); Optional<Family> findById(Long id); List<Family> findByUserNickname(String nickname); List<Family> findByUserId(Long userId); List<FamilyMember> findMembers(Long familyId); boolean isMember(Long familyId, Long userId); FamilyMember addMember(Long familyId, String nickname, String role); FamilyMember addMemberUserId(Long familyId, Long userId, String nickname, String role); boolean updateMemberRole(Long familyId, Long memberId, String role); boolean removeMember(Long familyId, Long memberId); boolean deleteFamily(Long familyId); }
